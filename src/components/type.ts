type user = {
    userName: string;
    userPassword: string;
    email: string;
    phone: string;
};

type address = {
    port: number;
    family: string;
    address: string;
};

export { user, address };
